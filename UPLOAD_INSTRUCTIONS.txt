CO PILOT COLLECTIONS LIVE — R8N19.11
TWILIO SILENCE / APPLICATION ERROR FIX

FILES IN THIS PATCH: 4

1) Upload:
   supabase/functions/voice-broadcast/index.ts

2) Upload:
   supabase/functions/voice-broadcast-webhook/index.ts

3) Upload:
   supabase/config.toml

4) Redeploy BOTH Supabase Edge Functions:
   - voice-broadcast
   - voice-broadcast-webhook

IMPORTANT SUPABASE SETTING
The public Twilio webhook must NOT require a Supabase JWT.
Confirm voice-broadcast-webhook has Verify JWT disabled.
The included config.toml contains:

[functions.voice-broadcast-webhook]
verify_jwt = false

WHY THIS FIX WORKS
- The first greeting is now supplied directly to Twilio as inline TwiML.
- The call no longer depends on the webhook successfully loading before the greeting plays.
- A 2-second pause is included before speech.
- Press 1 and transfer callbacks still use voice-broadcast-webhook.

TEST
Create a NEW one-account campaign and start it.
Expected flow:
1. Phone rings.
2. Two-second pause.
3. Full message plays from the beginning.
4. Press 1.
5. RingCentral queue rings.
6. Live-transfer screen pop appears.
